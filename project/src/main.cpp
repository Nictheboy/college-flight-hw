#include <iostream>
#include <string>
#include <planner.hpp>

int main() {
    Planner planner;

    while (true) {
        std::string query;
        std::getline(std::cin, query);
        /*
        
        */
    }
    return 0;
}
